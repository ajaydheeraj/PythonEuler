# def isprime(n):
#     for i in xrange(2,int(n**0.5)+1):
#         if n % i == 0:
#             return False
#     return True

# l = []
# for n in range(2,2000000):
#     if isprime(n):
#         l.append(n)
# print sum(l)

def sieve(n): # all primes under number
    l = [True] * (n+1) # set up grid
    l[0] = l[1] = False# mark 0 and 1 as not prime
    for i in xrange(2,int(n**0.5)):# loop up to the sqrt of n
        if l[i]:#if l[i] is true
            for a in xrange(i*i,n+1,i):#all remaining increments of i
                l[a] = False#mark them all false
    return [i for i,j in enumerate(l) if j == True]# return the values of the prime numbers

print sum(sieve(2000001))
